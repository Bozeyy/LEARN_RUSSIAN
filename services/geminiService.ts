
// Service IA désactivé pour assurer la stabilité du build Railway sans clé API.
export async function getWordExplanation(word: string, context: string) {
  return "L'assistant IA est actuellement désactivé.";
}
